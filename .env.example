# SOCKS5 proxies with credentials (comma-separated)
# Use this instead of config.yaml if your proxies contain passwords
TWITTER_PROXIES=socks5://user:pass@host1:port,socks5://user:pass@host2:port

# API Keys
# OpenAI (required if using openai provider)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Generative AI (required if using google provider)
GOOGLE_API_KEY=your-google-api-key-here

# SMTP Credentials
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password

# Database Credentials (optional - for pgvector production use)
# POSTGRES_URL=postgresql://user:password@localhost:5432/sentiment_db
