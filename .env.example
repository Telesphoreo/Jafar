# Google Gemini API (required - used for LLM analysis and embeddings)
GOOGLE_API_KEY=your-google-api-key-here

# PostgreSQL Database (required - includes pgvecto.rs vectors extension)
DATABASE_URL=postgresql://user:password@localhost:5432/jafar

# SMTP Credentials (required - for sending digest emails)
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your-app-specific-password

# Twitter Proxies (optional - comma-separated, can also be set in config.yaml)
# TWITTER_PROXIES=socks5://user:pass@host1:port,socks5://user:pass@host2:port
