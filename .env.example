# =============================================================================
# Twitter/X Credentials (for twscrape)
# =============================================================================
# NOTE: twscrape requires accounts to be added via CLI before running the script.
#
# 1. Create a file called accounts.txt with your credentials:
#    username:password:email:email_password
#
# 2. Add accounts from the file:
#    twscrape add_accounts accounts.txt username:password:email:email_password
#
# 3. Login all accounts:
#    twscrape login_accounts
#    (Use --manual flag if your email doesn't support IMAP)
#
# 4. Verify accounts are ready:
#    twscrape accounts
#
# The accounts are stored in a SQLite database (accounts.db by default).
#
# These environment variables are optional - only needed if you want to add
# accounts programmatically during initialization:
TWITTER_USERNAME=your_twitter_username
TWITTER_PASSWORD=your_twitter_password
TWITTER_EMAIL=your_email@example.com
TWITTER_EMAIL_PASSWORD=your_email_password

# =============================================================================
# LLM Provider Configuration
# =============================================================================
# Choose your LLM provider: "openai" or "google"
LLM_PROVIDER=openai

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o

# Google Generative AI Configuration
GOOGLE_API_KEY=your-google-api-key-here
GOOGLE_MODEL=gemini-1.5-pro

# =============================================================================
# SMTP Email Configuration
# =============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password
SMTP_USE_TLS=true

# Email Recipients
EMAIL_FROM=your_email@gmail.com
EMAIL_TO=recipient@example.com
# For multiple recipients, use comma-separated values:
# EMAIL_TO=recipient1@example.com,recipient2@example.com

# =============================================================================
# Application Settings
# =============================================================================
# Number of tweets to fetch per broad topic
BROAD_TWEET_LIMIT=50

# Number of tweets to fetch per trending entity
SPECIFIC_TWEET_LIMIT=20

# Number of top trending entities to analyze
TOP_TRENDS_COUNT=5

# twscrape database file path (default: accounts.db in current directory)
TWSCRAPE_DB_PATH=accounts.db

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
